# FolioHook 仓库协作约定

## 开始与恢复

1. 先读 [项目任务书](docs/项目任务书.md) 和 [执行看板入口](docs/执行看板.md)，从管理任务提供的唯一入口读取本机活看板。
2. 结合 [README](README.md) 和 [技术合同](ARCHITECTURE.md) 理解现有行为；历史阶段记录不替代本轮授权和验收。
3. 核对当前工作树、分支、未提交改动及本任务负责范围。恢复对话或上下文压缩后，也要核验看板所述的文件、进程和连接实际状态，不能只按旧聊天继续。

用户常用语音输入；结合上下文理解错别字和专有名词。优先最小、可维护改动，保持现有架构；不顺手重构或修复旁支问题。技术名词用简明中文解释。

## 任务与共享资源

- 管理任务维护唯一活看板，分配范围、接收证据和安排交接；其他任务提交结果给管理任务，不另建一份活看板。
- worktree（独立工作树）只隔离代码，不隔离 HOME、Codex 全局配置、钥匙串、应用数据、端口、浏览器或外部服务。
- 同一共享配置同一时刻只有一个明确的写入负责人。其他任务需要操作时，先经管理任务协调交接；普通的已授权局部编辑不增加逐项审批。
- 共享资源登记是协作约定，不是技术锁；不能阻止其他项目、客户端或后台进程写入，也不能据此证明配置未变。

## 真实共享配置的变更

操作前核对既有用户授权、精确目标、负责人与相关前态；不扩大到其他连接或设置。前态与结果应留下可持久化的脱敏记录，包含字段路径、用途、负责人、时间、预期变更和验证结论。

涉及前后比较时，要保留新增、移除、变更字段的脱敏差异，不能只断言“相等”、失败后丢失全部差异证据。秘密字段只记字段路径和变化类别，不记录值；不要把含秘密的配置原文备份到项目、Git、日志或报告。无法安全记录的内容明确标为未观测。

发现预期外差异时，停止该共享配置的进一步写入，报告已知事实与证据缺口，由管理任务协调下一步。不得猜测原因、覆盖配置或擅自回滚未知差异。

## 交付与边界

- 完成记录写清改动、证据位置、验收层级、遗留问题和下一步；不复制整段聊天。
- 分开表述“实现完成”“离线通过”“真实链路通过”“用户带练完成”，每项结论绑定具体版本、实例与证据。
- 真实凭据、付费模型、客户端共享配置、Tunnel、发布和真实数据删除遵循用户已给的具体授权；已有授权不重复索取，尚缺的必要用户操作交给用户。
- 仅使用本轮生成的合成资料或明确授权的来源。旧“能量信息学”工程仅作只读思路参考，不复制其私人文献库、数据库、Key 或 Tunnel 资产。
- 本机状态与脱敏报告放 `local-artifacts/`，先确认其被 Git 忽略，禁止强制提交。公开文档不写用户绝对路径、内部任务 ID、秘密或实时端口状态。
- 不自动 commit、push、打包、发布或清理真实资料；完成当前授权范围后交接。
